#include <iostream>
using namespace std;

int main() {
	setlocale(0, "");
	int sum = 0;
	int number;
    int i = 0; 

	while (true) {
		cout << "Введите число: ";
		cin >> number;
		i++;
		sum = number + sum;
		if (number == 0) {
			break;
		}
	}
	    cout << "Количество чисел:" << i << endl;
		cout << "сумма:" << sum;
		return 0;
}
