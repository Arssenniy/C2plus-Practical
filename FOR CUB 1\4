#include <iostream>
using namespace std;

int main() {
	int h;
	cin >> h;
	int b = h / 2;
	int p = h - b ;
	for (int i = 0; i < h; i++ ) {
		for (int a = 0; a < h; a++) {
			if (i >= p && a >= p) {
				cout << "o ";
			}
			else {
				cout << ". ";
			}
		}
		cout << endl;
	}
	return 0;
}
