#include <iostream>
using namespace std;

int main() {
	setlocale(0, "");
	case 3:
		system("cls");
		int number;
		cout << "| + | Multiply the array\n";
		cout << "| + | Enter nuber: ";
		cin >> number;
		for (int b = 0; b < N; b++) {
			for (int i = 0; i < N; i++) {
				massiv[i] = number * massiv[i];
			}
		}
		system("cls")
		cout<<"| + | Massiv: "
			for (int i = 0; i < N; i++) {
				cout << massiv[i] << " ";
			}
		break;
}
